import React from "react";
import { StyledEditForm } from "./EditForm.styles";

const ConfirmationModal = () => {
  return (
    <StyledEditForm>
      <strong className="subTitle">Inheritance Deleted Successfully!</strong>
    </StyledEditForm>
  );
};

export default ConfirmationModal;
